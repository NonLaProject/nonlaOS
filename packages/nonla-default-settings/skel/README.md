# nonla-default-settings source placeholders

Thư mục này giữ chỗ cho cấu hình mặc định sẽ được cài vào `/etc/skel` trong
các bước sau.

Hiện tại package chưa ship payload thật để tránh cài các thư mục rỗng vào
`/etc/skel`.
